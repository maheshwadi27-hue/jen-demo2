	pipeline {

    agent any

    stages {

        stage('Checkout Code') {
            steps {
                git 'https://github.com/maheshwadi27-hue/jen-demo2.git'
            }
        }

        stage('Build') {
            steps {
                sh 'echo "Building project..."'
                sh 'ls -l'
            }
        }

        stage('Test') {
            steps {
                sh 'echo "Running tests..."'
            }
        }
    }
}
